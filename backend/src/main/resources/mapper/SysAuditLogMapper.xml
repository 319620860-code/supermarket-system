<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermarket.backend.mapper.SysAuditLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_audit_log (username, action, detail, ip, create_time)
        VALUES (#{username}, #{action}, #{detail}, #{ip}, NOW())
    </insert>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*) FROM sys_audit_log
        <where>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="usernameLike != null and usernameLike != ''">
                AND username LIKE CONCAT('%', #{usernameLike}, '%')
            </if>
        </where>
    </select>

    <select id="selectPageByCondition" resultType="com.supermarket.backend.entity.SysAuditLog">
        SELECT id, username, action, detail, ip, create_time AS createTime
        FROM sys_audit_log
        <where>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="usernameLike != null and usernameLike != ''">
                AND username LIKE CONCAT('%', #{usernameLike}, '%')
            </if>
        </where>
        ORDER BY create_time DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
